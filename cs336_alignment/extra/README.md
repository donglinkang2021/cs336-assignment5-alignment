# A Simple Json Viewer

For better evaluate the result of the model or the original datasets, just write a server to render the latex code or text for each row of the data.

To launch the server:

```bash
bash scripts/launch_server.sh
```